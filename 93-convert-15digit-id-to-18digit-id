public String convert15to18DigitId(String fifteenDigit)
{
    String res;
    Id ids = valueOf(fifteenDigit);
    String eighteenDigit = valueOf(ids);
    Integer i = fifteenDigit.Length();
    //code here
   ids =  valueOf(fifteenDigit, restoreCasing);
    if( i < 15)
    {
        res= '-1';
    }
    if( i == 15)
    {
      res = ids;
    }
    
    return res;
}