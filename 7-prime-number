public Boolean isPrime(Integer num) {
    //code here
    Boolean res;
    For( Integer i = 2 ; i < num ; i++)
    {
        if(Math.mod(num, i ) != 0) 
        {
            res = true;
        }
        else
        {
            res = false;
        }
    }
    return res;
}