public String reverseWordsInASentence(String sentence){
    //Convert the string to a list of characters
    List<Character> charList = New List<Character>(sentence.toCharArray());
    // reverse the list
    charList.reverse(); 

    //Join the characters back into a string

    String rev = String.join(charList, '');
    return rev;
}