public Boolean isTypeAccount(sObject record)
{
    // code here
    Boolean res;
    Account A = New Account();
    if(record.get(0) instanceof Account )
    {
        res = true;
    }
    else
    {
        res = false;
    }

  return res;
    
}