public Boolean isTypeAccount(sObject record)
{
    // code here
    Boolean res;
    sObject record = New sObject();
    if(instanceof record)
    {
        res = true;
    }
    else
    {
        res = false;
    }

  return res;
    
}