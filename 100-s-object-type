public Boolean isTypeAccount(sObject record)
{
    // code here
    Boolean res;
    sObject record = New sObject();
    if(record.instanceof Account)
    {
        res = true;
    }
    else
    {
        res = false;
    }

  return res;
    
}