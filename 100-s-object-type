public Boolean isTypeAccount(sObject record)
{ 
    Boolean res;
    if(record == Account)
    {
        Account A  = New Account(name);
        A.Name = name;
        return res = true;
    }
    else
    {
        return res  =false;
    }
    // code here
}