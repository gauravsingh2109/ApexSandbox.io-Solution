public Boolean isTypeAccount(sObject record)
{
    // code here
    Boolean res;
    List<sObject> A = New List<sObject>();
    record = New Account();
    A.add(record);
    if(A.getSObjectType(0) instanceof Account)
    {
        res = false;
    }
    else
    {
        res = true;
    }
    
    return res;
}