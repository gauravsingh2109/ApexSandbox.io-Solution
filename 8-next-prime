public Integer nextPrime(Integer num) {
    //code here
    Integer k;
    Integer flag;
    Integer res;
    For( k = num; k> num; k++)
    {
        For(Integer i = 2; i<k; i++)
        {
            if(Math.mod(k,i) != 0)
            {
               flag = 1;
            }
        }
    break;    
    }
    
    if(flag == 1)
    {
        res = k;
    }
   return res; 
}
    
